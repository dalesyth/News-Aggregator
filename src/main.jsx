import React from "react";
import ReactDOM from "react-dom";
import { Title, App } from "./components";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Title />
    <App />
  </React.StrictMode>
);
